/**
 * PaletteLive - Heatmap (DEPRECATED)
 *
 * This content script is no longer injected. The heatmap window now receives
 * its data directly from the popup's existing scan results (passed via
 * chrome.storage.session) and uses EXTRACT_PALETTE for refreshes.
 *
 * Kept for reference only — can be safely deleted.
 */
